#include "push_swap.h"

void	ft_compute(t_stack *a, t_stack *b)
{
	// if (ft_rotate_sorted(a))
	// {
	// 	return ;
	// }
	// if (a.size <= 7)
	// 	ft_sort_small(a, b);
	// else
	ft_sort_big(a, b);
}
